export default function Loading() {
  return <h1>Loading</h1>;
}

// * you can display loading state immediately, application becomes more responsive
// * Very useful in components with shared layouts
